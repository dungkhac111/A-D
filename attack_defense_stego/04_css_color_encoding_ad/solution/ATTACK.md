# H??ng T?n C?ng

1. M? service.
2. Ki?m tra probe `/debug/theme?name=internal`.
3. T?m d? li?u public b? l? theo k? thu?t `Path traversal + CSS color encoding`.
4. D?ng `solution/exploit.py` ?? t? ??ng h?a.

```bash
python3 solution/exploit.py http://target:8087 <flag_id>
```
