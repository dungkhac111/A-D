# Gi?i Tr?nh Checker

Checker chu?n Attack/Defense cho b?i `QR Shadow Layer AD`.

- `check`: ki?m tra `/healthz`, giao di?n public, probe public v? b?o v? endpoint checker.
- `put`: nh?n flag t? h? th?ng, ??t flag ??ng v?o service, ch? in `flag_id`.
- `get`: ??c l?i flag theo `flag_id` v? so s?nh v?i expected flag.

Exit code: `101 OK`, `102 CORRUPT`, `103 MUMBLE`, `104 DOWN`, `110 CHECK FAILED`.
